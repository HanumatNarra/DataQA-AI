export const colors = {
  primary: 'var(--primary)',
  surface: 'var(--surface)',
  surface2: 'var(--surface-2)',
  line: 'var(--line)',
  text: 'var(--text)',
  textMuted: 'var(--text-muted)'
} as const

export const urls = {
  // Placeholders for future centralization of external URLs
} as const


